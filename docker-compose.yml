version: "3"
services:
  rabbit:
    image: rabbitmq:3-management
    ports:
      - 5672:5672
      - 15672:15672

  clipper:
    build: .
    ports:
      - 5000:5000
    depends_on:
      - rabbit
    command: python -m pyclipper.main
    volumes:
      - .:/opt/clipper
